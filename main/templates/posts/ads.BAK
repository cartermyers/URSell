Here is a test page.

{% if posts %}
  {% for post in posts %}
    {{ post.title }}<br />
    {{ post.poster }}<br  />
    {{ post.category }}<br  />
    {{ post.price }}<br  />
    {{ post.description }}<br  />
    {{ post.offering }}<br  />

    {% for i in post.postimages_set.all %}
    <img src="{{ i.image.url }}" alt="no image">
    {% endfor %}
    
    <br />
  {% endfor %}

  <div class="pagination">
      <span class="step-links">
          {% if posts.has_previous %}
              <a href="?page={{ posts.previous_page_number }}">previous</a>
          {% endif %}

          <span class="current">
              Page {{ posts.number }} of {{ posts.paginator.num_pages }}.
          </span>

          {% if posts.has_next %}
              <a href="?page={{ posts.next_page_number }}">next</a>
          {% endif %}
      </span>
  </div>

{% else %}
  Sorry! There's nothing here.
{% endif %}
